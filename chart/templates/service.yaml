apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payments-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: payments-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
